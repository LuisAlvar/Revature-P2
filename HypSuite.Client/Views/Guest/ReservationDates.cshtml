@model HypSuite.Domain.Models.Reservation

<form action="/Guest/ReservationDates" method = "post">
  <div class="form-group">
      <label for="CheckIn">Check-In Date:<br>(Format: mm-dd-yyyy)</label>
      <input type="date" class="form-control" id="CheckIn" maxlength="8" name = "CheckInDate" pattern="[1-12]{2}-[1-31]{2}-[2020-2021]{4}" required>
  </div>

  <div class="form-group">
      <label for="CheckOut">Check-In Date:<br>(Format: mm-dd-yyyy)</label>
      <input type="date" class="form-control" id="CheckOut" maxlength="8" name = "CheckOutDate" pattern="[1-12]{2}-[1-31]{2}-[2020-2021]{4}" required>
  </div>

  <button type="submit" class="btn btn-primary">Create Location</button>
</form>